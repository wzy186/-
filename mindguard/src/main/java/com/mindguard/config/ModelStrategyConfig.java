package com.mindguard.config;

import org.springframework.context.annotation.Configuration;

/**
 * ChromaVectorStore is auto-configured by spring-ai-chroma-store-spring-boot-starter.
 * No manual bean definition is needed; the starter provides the VectorStore bean
 * based on application properties (spring.ai.chroma.*).
 */
@Configuration
public class ModelStrategyConfig {
}
